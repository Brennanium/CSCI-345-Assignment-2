package deadwood.model.areas;

import java.util.ArrayList;

import deadwood.model.*;

public class Trailers extends Area {
    public Trailers(String name, ArrayList<Area> neighbors, Role[] roles) {
        super(name, neighbors, roles);
    }
}
